import { makeStyles } from '@material-ui/core/styles';

export default makeStyles({
    editPopoverPaper: {
        padding: 5,
        display: "flex",
        flexDirection: "column"
    },
    optionButton: {
        marginBottom: 10
    }
});